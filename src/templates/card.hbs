

<div class="card">
		<h2>
			<img src="{{this.flags.svg}}" alt="{{name}}">
		 {{name}}
		 </h2>
		<p><b>Capital:</b> {{capital}}</p>
		<p><b>Population:</b> {{population}}</p>
		<p><b>Languages:</b>{{#each languages}} {{name}}. {{/each}}</p>
</div>
